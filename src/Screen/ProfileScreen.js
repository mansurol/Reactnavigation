import { View, Text, SafeAreaView } from 'react-native'
import React from 'react'

export default function ProfileScreen() {
  return (
    <SafeAreaView>
      <Text style={{alignSelf:"center",padding:30}}>ProfileScreen</Text>
    </SafeAreaView>
  )
}